import { SpecifiedMatch } from './specifiedMatch';
import { Usermessage } from './Usermessage';

// tslint:disable-next-line: no-unused-expression
export interface Pronostics {
      matchs?: SpecifiedMatch;
      message?: Usermessage;
    }
